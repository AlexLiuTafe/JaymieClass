using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// UI
using UnityEngine.UI;

// NameSpace
namespace DelayHealth
{
    // Component Menu
    [AddComponentMenu("Intro PRG/RPG/Player/Health - Delay")]
    public class Health : MonoBehaviour
    {
        // Player health max and current and delay

        // dropSpeed

        // delay slider (background slider), current health slider
        // delay fill, current fill 


        void Update()
        {
            // setting of current health value on slider
            // if delay health is bigger than our current health
            // - over time according to our speed make delay health match the current health
            // setting of delay health value on slider

            // Healthmanager()
        }

        //HealthManager()
        // if Control the display of the current health fill
        //	- off and on
        // if Control the display of the delay health fill 
        //	- off
        //	- on ...set delay value to current health
        //	- on ...set slider to current health
    }
}

	// two sliders exactly the same set up (set first background slider duplicate it)
	// parent the new slider to the duplicated slider.
